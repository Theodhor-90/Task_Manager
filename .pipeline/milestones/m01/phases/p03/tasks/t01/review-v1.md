{"verdict":"needs_revision","feedback":"Core plugin implementation is largely aligned (new JWT/CORS plugins, async `buildApp`, config and tests updated), and targeted checks pass (`npx vitest run packages/server/test/app.test.ts`, `npm run build -w packages/server`). However, the implementation does not fully match the locked plan/spec verification requirements.","issues":[{"file":"packages/server/package.json","description":"Locked plan step 1 requires `fastify-plugin` as a direct dependency, but it is missing from `dependencies` even though `jwt.plugin.ts` and `cors.plugin.ts` import it. Current behavior relies on transitive resolution and is not plan-compliant."},{"file":"packages/server","description":"`npm run test -w packages/server` fails (37 failing tests), so the spec/plan criterion \"All pre-existing tests still pass\" is not met."},{"file":"packages/server/package.json","description":"`npm run dev` fails (`Cannot find module .../packages/server/watch` from `tsx watch src/server.ts`), so the verification criterion that dev server starts without errors is not met."}]}