{"verdict":"needs_revision","feedback":"Build and type-check pass, and required files exist, but the implementation does not match the locked plan’s approved `auth.ts` behavior for `login()` error handling.","issues":[{"file":"packages/client/src/api/auth.ts:8","description":"`login()` uses `apiClient.post(...)`, which applies the global 401 interceptor. This conflicts with the locked plan contract (plan-locked.md lines 142-173, 212-216) that explicitly required direct `fetch` for login so invalid credentials surface as server error messages (e.g., \"Invalid credentials\") instead of forced `Error(\"Unauthorized\")` redirect behavior."}]}