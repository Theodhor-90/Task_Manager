{"verdict":"needs_revision","feedback":"Implementation largely matches the locked plan and all verification commands passed (install, build, typecheck, dist artifact checks, workspace resolution, and export assertions). However, it does not fully satisfy the task spec exit criteria: the spec requires an exported `ApiError` contract type, but the implementation exports only `ApiErrorResponse`.","issues":[{"file":"packages/shared/src/types/index.ts","description":"Missing `ApiError` type required by spec.md. Only `ApiErrorResponse` is defined."},{"file":"packages/shared/src/index.ts","description":"Barrel exports do not expose `ApiError` as required by spec.md; only `ApiErrorResponse` is exported."}]}